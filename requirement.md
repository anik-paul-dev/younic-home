
must very strictly read, observe and analysis all line by line and follow what i said below and then do properly. then provide complete website and don't provide any fractional or incomplete code at all.

build me a dynamic mini hostel(name: Younic Home) management system (customer side) where users can manage seat changes, rent, leave an exit requests through a web app. Basically a hostel website where users can book and manages seat. must remember that website refund policy where user cannot get back payment if they want to cancel seat and can change manage other seat as per logic.

core features to develop:
1. seat change request system
-users will be able to request seat change from their current seat.

Types of seat change:
-Same branch (e.g. 2-seat room to 3-seat room)
-Different branch (Branch A- Branch B)

Payment Adjustment logic:
-if user has extra balance, it will be adjusted with next month's rent.
-if user needs to pay extra. system should show payable amount.
(add logic so that user can pay through Baksh/Nagad/visa dynamically and payment status must have to update dynamically when pay payment or updated payment and send notification to user and admin in real-time. so for now use custom stripe payment system as test purpose)

Requirement:
system must dynamically calculate and display payment differences.

2. customer profile system:
-each user will have a personal dashboard/profile.

profile includes:
-Basic info (name, phone Nid/Id)
-current seat and room details.
-branch information.

Requirement:
-profile data should be structured and properly managed(editable where applicable)

3.Rent management
-Monthly rent notification before due date
-payment history
-payment status (paid/due)

Requirement:
-system should be dynamically update payment status based on transaction.

4.Leave Application System:
-user can apply for leave from the app.

Features:
-select duration(start date - end date)
-Mention reason
-admin approval/rejection

Requirement:
-status must update dynamically (Pending, Approved/ Rejected).

5. Exit Application System
-user can request to leave the hotel.

Features:
-Notice period validation.
-Exit request submission.
-Final settlement calculation (rent + deposit adjustment)

Requirement:
-system must calculate final payable/refundable amount automatically

6. Notification system
-rent reminders
-seat change updates.
-leave/Exit status updated.
-general announcement.

Requirement:
-notification system must be dynamic (real-time preferred) here use socket.io for real tine notification.

Technical Requirements:
-system must be fully dynamic (no hardcodes values)
-proper database design (users, rooms, payments, requests, etc.). must maintain primary/foreign key and indexing where need.
-clean and user friendly interface.
-API-based architecture preferred.
-proper Admin panel and user panel.
-use proper and accurate login/register page where both admin and user can properly login/register. used all properly and authentic and professional logic to build this website. use must PHP/Laravel and MySQL database. also for notification use socket.io. i already created project folder of Laravel for this project and used Laravel update version 11.54.0 including all essentials files. so maintain properly structure of Laravel. additionally, my current, Composer version is 2.9.5 and PHP version is 8.2.12, Laravel Framework 11.54.0 and i used xampp. also i used windows 11. secrete Laravel project and then build the website properly.

#note:
also must maintain clean code quality and structure so that if any developer see the code then understand the system and codes. so follow also problem-solving approach.

Younic Home is hostel management system in Dhaka, Bangladesh. so must build the full website with proper color and dynamic interface for best user experience.

so then must provide complete website including all files codes and don't provide any fractional or incomplete code at all. must maintain secure code so that not break and also must remember that so that no error occur. also must remember do not try to create too much unnecessary files and try to write efficient code. so must make sure do not create so much files tall all because this can be write in some files in efficient way and so don't use so much excessive file at all. must follow efficient and simple file structure without creating too much files for this simple website. so do not even try to explain too much at all instead of write codes correctly, i just need to accurate code not explanation.